import { TILE_SIZE } from '../../config';

export const GAME_TIMEOUT = 150;
export const COIN_SCORE = 50; // TODO: Move to somewhere else
export const METALLIC_BLOCK_TILE = 44; // TODO: Move to somewhere else
export const PLAYER_START_X = TILE_SIZE * 6;
